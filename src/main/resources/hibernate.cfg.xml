<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- JDBC Connection -->
        <property name="connection.url">jdbc:postgresql://localhost:5432/lunanotes</property>
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.username">developer</property>
        <property name="connection.password">devpass</property>

        <!-- Hibernate Settings -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>
        <property name="hbm2ddl.auto">update</property> <!-- create, update, validate -->

        <!-- Entity -->
        <mapping class="com.lunanotes.model.Note"/>

    </session-factory>
</hibernate-configuration>