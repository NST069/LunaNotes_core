spring:
  datasource:
    url: jdbc:h2:mem:lunanotes
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true

api:
  endpoint:
    base-url: /api/v1

security:
  oauth2:
    resourceserver:
      jwt:
        authorities-claim-name: authorities
        authority-prefix: ""